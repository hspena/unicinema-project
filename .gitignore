# Environment variables (API keys / secrets)
.env
.env.local
.env.*.local

# Firebase service account / admin SDK keys — never commit these
*firebase-adminsdk*.json
serviceAccountKey.json

# Production build
/build

# Misc
.DS_Store
