# Environment variables (API keys / secrets)
.env
.env.local
.env.*.local

# Firebase service account / admin SDK keys — never commit these
*firebase-adminsdk*.json
serviceAccountKey.json

# Dependencies (install locally via `npm install`)
/node_modules

# Production build
/build

# Misc
.DS_Store
